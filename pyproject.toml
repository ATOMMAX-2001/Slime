[project]
name = "slime"
version = "0.1.0"
description = "A Simple webframework lib for python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "maturin>=1.12.4",
    "watchdog>=6.0.0",
]
