[project]
name = "slime"
version = "0.1.0"
description = "A Simple webframework lib for python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "maturin>=1.12.4",
    "pydantic>=2.12.5",
    "uvloop>=0.22.1",
    "watchdog>=6.0.0",
]
